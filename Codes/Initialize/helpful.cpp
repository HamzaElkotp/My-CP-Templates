// fill vector with values starting from x to n
iota(allv(vct), x);


// Find in string
str.find('x') != variant_npos;


// Get char to the line end
int c = 0;  
while((c=getchar()) != '\n' && c != EOF){};


// String to Int
stoi(str);

// Integer to string
to_string(num);


// Random Value
mt19937 rng(1337)